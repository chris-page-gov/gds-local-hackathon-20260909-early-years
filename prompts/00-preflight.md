# Astra preflight — run in the actual Codex workspace

## Task

Prepare the smallest working, inspectable OKF+ demonstration for the Early Years Hackathon on Wednesday 9 September 2026. The primary implementation is the existing `chris-page-gov/okf-explorer` repository. This preparation kit is a rehearsal and integration aid, not a competing platform.

Read this kit's README, AGENTS.md, profile/contract.json and docs/upstream-integration.md. Inspect the current workspace's governing instructions and repository state before any edit. Do not assume this kit or an inherited skill overrides higher-priority instructions or managed policy.

## Record, do not guess

Write `preflight.json` in the agreed local working directory with: working tree location, branch and HEAD; dirty paths; installed Codex version; selected model; effective reasoning level; evidence for that selection; sign-in/API mode without secrets; enabled skills and any instruction conflicts; available browser and Python/Node environments; network policy; permitted source data; publication authority; paid API budget; available consumers and their versions.

Use `unknown` where the host does not expose a value. A model's conversational claim about its identity is not runtime evidence. Do not quietly substitute another model and call it Astra. Verify availability in the actual workspace. Consult current official OpenAI documentation when settings differ from the preparation snapshot.

## Authorised outcome

Read-only inspection, isolated local changes, compilation, tests and a local reviewable handoff are authorised for the supplied fictional fixture. Preserve unrelated work. Do not push, create a remote/PR, publish, spend paid API funds, change access controls or process newly supplied real personal data without separate authority.

Ask only when an unresolved decision blocks that boundary or changes the intended outcome. Continue other authorised work. Do not ask for approval merely to inspect or validate the local fictional fixture. Do not begin a fresh standards survey when the existing source register already settles the question.

## Work order

1. Confirm the immutable kit inputs and run its local check/test commands.
2. Inspect the real Svelte Explorer's supported bundle-loading mechanism.
3. Run the exact 22-concept/23-relationship fixture through the actual consumer, including one corrupted copy. Record results, not a mock substitute.
4. Produce the smallest adaptation needed to integrate with the existing pipeline. Follow the pinned upstream profile rules rather than weakening them.
5. Use the semantic research prompt only for a genuine unresolved design fork; use the event-data prompt only when actual event sources arrive.

One writer may coordinate at most two bounded read-only research/review workers. Keep explicit output ownership. Prefer deterministic tools for transforms, counts, hashes and tests. A fallback preview does not satisfy the upstream integration gate.

## Finish with

Exact changed files; model/configuration evidence; test receipts; a working local demonstration or precisely identified blocker; and one next owner decision only if necessary. Do not finish with an offer to begin the work.
