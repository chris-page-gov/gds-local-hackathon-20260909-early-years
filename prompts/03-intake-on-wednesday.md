# Wednesday intake — adapt without restarting the project

We have new hackathon source files. Work within the current approved environment and event rules. Preserve the existing fictional rehearsal unchanged.

Start by confirming the decision question and source-owner statement. Inventory and hash the exact files read-only. List source families, source-native fields, identifiers, dates, status values, missingness meanings and extraction gaps. Classify what is allowed for local processing, model processing and presentation separately. Ask only for genuinely blocking authority or scope information; continue unrelated permitted work.

Create a new immutable snapshot directory and a new adapter contract. Do not edit the fixed example counts, identifiers or tests to make the new data appear equivalent. Reuse the generic validation functions and canonical upstream implementation where possible.

Select two records, one evidenced relationship and one negative case. Produce readable OKF notes and source locators. Preserve original facts; mark unresolved mappings explicitly. Do not introduce new matching algorithms, national identifiers, clinical interpretations, statutory eligibility or live referrals.

Run the actual pinned consumer on that tiny slice. Expand only after source reconstruction, candidate/action-state safety and reader checks pass. Keep new semantic decisions in a versioned ledger; do not overwrite the approved profile through an informal chat instruction.

End with the new snapshot ID, field mapping, exact resulting artefacts, test receipts and one unresolved blocking question at most. State which parts are still the fictional preparation demo and which derive from the event inputs.
