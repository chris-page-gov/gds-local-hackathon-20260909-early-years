# Integrate without creating a competing OKF platform

## Local execution on 5 September 2026

The actual pinned Svelte consumer has been exercised with the exact 22-concept/23-relationship JSON. The [public acceptance summary](../evidence/acceptance.json) records the consumer revision, material hashes, runtime schema validation, browser results and remaining production gates. The [integration guide](local-integration-run.md) provides portable reproduction commands. The preparation inspection lock retains its original historical status.

## Baseline

Inspected Explorer commit: `c8af0b05cab49a5341e0b787e17d49a674868d3a`. Its canonical application is `apps/okf-explorer/`, not the legacy viewer. The source register records the precise code and documents inspected.

This kit has an **independent experimental profile**. Its rich rows use the inspected `okf-relationship-assertion.v2` field vocabulary and its envelope follows the small-bundle pattern, but neither similarity nor our tests establish full upstream compatibility. The initial preparation inspected the upstream runtime schema; the subsequent acceptance run executed it against all 23 relationship rows. The complete frozen Bundle Wiki v1 profile was not vendored. Never copy its canonical profile URI onto this subset.

## First local Codex task

Use an isolated worktree or copy of your local Explorer checkout, preserving unrelated work. Verify the commit, then read its AGENTS files, semantic contract, current integration tests and Python/Node locks. Keep the two repositories' environments separate. This kit now uses locked uv with Python 3.12.11; its original preparation used Python 3.13. See the [migration receipt](../evidence/uv-migration.json) for the subsequent checks.

Load the exact `bundle/okf-bundle.json` into the **actual Svelte reader** through its supported import/source mechanism. Determine that mechanism from current code or working examples, not guessed URL query parameters. Test overview identity, 22 nodes, 23 relationships, H-01 competing candidates, A-01 state, direction, provenance disclosure, keyboard use and saved source/route restoration. Test a deliberately corrupted copy and record the actual failure behaviour. Do not present a schema-only or mock reader result as that run.

Run the exact upstream runtime assertion schema against all 23 rows with format checking. Where full Bundle Wiki conformance is desired, use its complete opaque 16-file vendor lock and canonical generator. Do not weaken that frozen schema to accept the rehearsal. Introduce a versioned extension with a new identity where candidate or temporal semantics need it.

Use the result to decide whether the event profile belongs as a producer fixture under the existing test infrastructure or in a small sibling producer repository. Prefer adaptation to duplication. The independent compiler/preview here are fallback teaching and experimental tools, not a proposed replacement for the existing stack.

## Reviewable change set

Propose, but do not publish without approval:

- a small early-years fixture and a profile applicability decision;
- a candidate-assertion semantic regression test;
- a dated Astra execution overlay linked from the existing Foundry kit;
- a beginner exercise comparing links, typed predicates and evidence;
- an actual-consumer receipt and explicit limitations.

Retain the generic Foundry prompts as the model-neutral source. Keep model-specific behaviour in the overlay. Preserve separate Foundry versus project release-gate catalogues. A non-release rehearsal mode must have its own clearly labelled acceptance boundary; it must not silently bypass production gates.

## Release safety

No Git remote, push, PR, site publication, model API spend or rights change is pre-authorised here. The original Explorer documentation licence is CC-BY-NC-4.0 and its code licence is MIT; do not flatten these into one invented licence. Confirm event rules and employer permissions before distributing a competition artefact.
