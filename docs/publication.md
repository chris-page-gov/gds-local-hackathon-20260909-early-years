# Publication scope

The owner authorised a public repository for this existing fictional rehearsal on
5 September 2026. [PUBLICATION.json](../PUBLICATION.json) records that decision
separately from the immutable preparation contract. This is public source and
experimental consumer evidence; it grants no operational data-processing authority.

The public tree uses an explicit `.gitignore` allowlist. Raw receipts, local paths,
Codex configuration, archives, caches, pinned consumer copies and private intake
folders remain outside Git. A public acceptance summary records exact material
hashes and executed checks; the original logs are retained locally.

The owner reconfirmed publication after migration to locked `uv` on the same day.
The original 124-file review is supplemented by the Python project manifest,
lockfile, interpreter pin and migration receipt; the two requirements files are
removed. No fixture content or additional private material is included.

The public repository does not itself enable a hosted demonstration. A local
loopback URL works only on the machine running its server. A future Pages demo
must host the reviewed bundle and its relative concept/source/profile assets,
then verify its exact URL in the canonical Explorer before advertising it.

Code and content have distinct licences. Preserve third-party attribution and
synthetic labels. Real event data require a separate authority decision, new
immutable snapshot and reviewed adapter; do not replace this fixture.

The evaluation reference answers are public. They remain useful for reproducible
checks, but an experiment must disclose their availability to models and cannot
claim that this repository supplies a secret test set.

## CI browser environment

The first published commit, `53cd1c21e22518d301e0cd8dace2f617a96a6bc8`,
retains the [initial uv migration receipt](../evidence/uv-migration.json).
Its recorded material hashes describe that candidate. The first hosted CI run
passed the compiler, unit, byte-integrity and package checks, then the Ubuntu
runner rejected the downloaded Chromium sandbox before preview navigation.

CI now locates the runner’s installed Google Chrome and retains sandboxing.
This uses the browser’s existing Ubuntu policy without changing host restrictions.
The local migration run already used sandboxed installed Chrome. Subsequent
workflow changes and hosted outcomes are recorded in Git history and GitHub
Actions; the original observations remain unchanged.
